name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v6

      - name: Restore
        run: dotnet restore src/StickyNotesMcp.slnx

      - name: Build
        run: dotnet build src/StickyNotesMcp.slnx -c Release --no-restore

      - name: Test
        run: dotnet test src/StickyNotesMcp.slnx -c Release --no-build
